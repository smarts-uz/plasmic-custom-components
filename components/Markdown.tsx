import ReactMarkdown from "react-markdown";

export default function Markdown(props:{markdown:string}){
    const defaultMarkdown = `#**Plasmic *CustomComponents*!!!!`;

    return  (
        'Markdown Markdown Smth anything'
    )
}